# Historical structure chemistry implementation

These files record coordinate-preserving CCD reconstruction and the composite/BIRD handling used during preparation. Their original data paths and release-specific orchestration must be adapted to the downloaded PDB sources; they are not a new universal PDB-to-benchmark command. The public instance and residue/atom selection metadata are in `datasets/`.
