# Numerical source data

This lightweight collection retains per-complex endpoints and summary tables for Figures 2–4. Full candidate-level ranking ledgers and coordinate pose banks remain outside the public code release. `index.json` identifies the included tables: `source_sha256` records the historical source, whereas `public_file_sha256` identifies the distributed file after removing deployment paths.
